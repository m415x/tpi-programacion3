// Interfaz que define la estructura de un Iproducto en la tienda
export interface IProduct {
    id: number;
    name: string;
    description: string;
    price: number;
    image: string;
    category: string;
}
